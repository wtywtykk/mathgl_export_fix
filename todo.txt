http://ubuntuforums.org/showthread.php?t=1862084
Device 0 (VID=0502 and PID=337d) is UNKNOWN.

============= NEW  =============

1. Use Hershey as built-in font ?!!
2. Export to X3D
3. JavaScript (WebGL, canvas) based on JSON + function for rotate, zoom, CalcXYZ(ask server).

var the_object;
var http_request = new XMLHttpRequest();
http_request.open( "GET", url, true );
http_request.send(null);
http_request.onreadystatechange = function () {
	if ( http_request.readyState == 4 ) {
		if ( http_request.status == 200 ) {
			the_object = JSON.parse(http_request.responseText);
		} else {
			alert( "There was a problem with the URL." );
		}
		http_request = null;
	}
};

4. GTK and WX window/widgets
5. Labels at TriCont()

6. Save primitives for frames (not bitmap) ??? -- see MGL_USEDRWDAT ==> frames in OpenGL mode
7. Enable several fonts at the same picture -- global std::vector<mglFont> + index in this set
8. Table(mreal x, mreal y, HCDT dat, char *xlabels, char *ylabels, char *stl) -- draw table with values (+ auto reduce font size?) -- format in [] like "[%.2g]".
9. Graph(mreal x, mreal y, names, styles, font, size); -- names = "node1[node2,node3,node4[node5,node6]]", styles -- the same + apply for subnodes if absent. Styles are colors (fill,border,line), dash (to subnodes), marks "dos^v><", arrows.
10. Legend in several columns
11. Add flag for drawing 3d arrows instead of 2d (S,D -- cube, T -- sq.pyramid, I -- square, O -- sphere???, A,K,V -- ???)

12. Use multi-threading in drawing (MathGL level) -- bug with primitives drawing --> pixel.cpp

13. Check gap for axis labels (at axis rotation)
14. Axis -- projection (ternary + quaternary)
15. 2D textures as one of standard way for coloring -- if '%' present in color scheme

16. curved text (in new sample) look not so good
17. Auto axis range for formulas, like AutoRange("y(x)") or AutoRange('x',"x(t)").

18. Add mglDataC for complex data -- main diff is reading/saving data

19. Drawing of saddle quadrangles

20. Test mglcgi

21. Add Evaluate() sample

+24. MGL testing system ???

============= DOCS =============

1. Update web_*.texi -- links
2. Text about UDAV (tabs,dialogs,hotkeys)
* known feature -- if you draw something in function then highlighted all similar objects
3. Add FAQ about MPEG/GIF and PDF
4. Examples of animation, u3d, x3d, js and so on

5. Docs about WriteJSON()

============= UDAV =============

00. Kind of plot in newcmd should save already entered arguments (by its name) + high group don't switch correctly for external command
01. Show plot at creation stage (if option is enabled -- can be long process!!!) + auto axis range or [-1,1] by default

02. Double click -> jump to string + setup dialog (optionally)

03. Dialog for InPlot(s):
{SubPlot,MultiPlot}	-- enable rotate,aspect,title
{ColumnPlot}		-- enable rotate,several
{StickPlot}		-- enable several
{InPlot}		-- enable rotate,aspect,title

04. QTreeWidgetItem -- ICON (func/call,if,for,once,subplot,...); annotation/name; (keep LINE - POS in script). After editing/changing --> put text to editor.
Buttons: new,load,save // newcmd,hide,annotation,collapse on/off,setup // calc. Click on item cause change of line number in editor.
05. Group/ungroup if command in single line, separated by ':'
06. Hide plot/group by putting "#h " at beginning of line
07. Annotation of plot/group -- comment before it

08. Drag&drop plot/group between inplots

09. Create default plot dependently of selected row/column/range in data table
10. Data change via menu/toolbar -- save to internal script => enable undo + save initial array(?)

11. Add primitives by mouse at beginning of script + change its size/position by mouse + "attraction" points
The primitives are: ball, line, ellipse/circle, rect, rhomb, text, curve. May be 3D ones: cone, sphere/drop, error-box

12. Add line numbers in editor.

13. Check disabling completer in UDAV

14. Zoom by mouse wheel + shift by right button

============= UNSURE ===========

1. Problem with \calB and so on (\calH, ...) -- they are present only in italic font :(.
2. create PWT fonts
3. \dfrac for normal size and centering  (sample \big\big{\frac{1}{2}}) ???
4. "Cut off" curves if text is printed inside it (for contour labels) ?!?
5. String variables in MGL + readstr command.
6. Pool of threads for speeding up ???
7. Read DICOM files
8. Check RunThr() in python/octave
9. Try libtcc (TinyCC) as alternative to MGL -- how to handle ^ operator???
