============= TEST =============
1. Triangulation
2. Text
	* export as text (EPS, SVG, TeX ...)
3. Axis
	* ternary, quaternary, projection (& ternary, quaternary projections -- change *_plot())
4. opengl
	* draw wire font too for OpenGL mode
5. stfa
6. Drop() -- rays

============= NEW  =============

01. BeltP() -- analogue of FlowP() but by belt -- fixed and variable width of belt. Color depend on vector field.
02. Box() + edges + half-transparent
03. parallelisation of drawing (MathGL level)
04. Export to LaTeX formats - (pgf/tikz) or metapost if possible (like EPS).
05. Export to CALLADA
06. Export to X3D
07. Export to JavaScript (WebGL)
08. GTK and WX window/widgets
09. Import/Export via GIF (as 3d data) ??? + 16bit PNG
10. Read DICOM files.
11. Contour label should not rotated on ~180 degree
12. Labels at TriCont()
13. Cones() -- like Bars() but by Cone()
15. Add Candle() plot -- see gmail.com
16. MPISend(), MPIReceive()
17. Add diffusive light (no dependence of normale but have direction)

============= UNSURE ===========

1. Arrows at flow lines ?!!
2. Problem with \calB and so on (\calH, ...) -- they are present only in italic font :(.
3. create PWT fonts
4. \dfrac for normal size and centering  (sample \big\big{\frac{1}{2}}) ???
5. "Cut off" curves if text is printed inside it (for contour labels) ?!?
6. Check if widget functions can be added to mgl_w.h !?! -- use #define ??!
7. Save ALL plot styles for each curve (0...N) for each subplot (0...M) in format \leg{N_M} ?!?
8. Add TeX command \leg{plot style} for line+mark of specified style.
9. Use Hershey as built-in font ???
10. String variables in MGL + readstr command.
11. Pool of threads for speeding up ???
12. Drops as mglPrim -- not accurate but smaller memory and faster
13. Text aspect ratio in OpenGL mode

============= DOCS =============

1. Write "Quick start" userguide
2. FAQ about sizes of plot (parametrical, or Min*Max)
3. FAQ about MinGW -- http://www.mingw.org/wiki/FAQ
4. MGL docs -- text what formulas are applicable for scalars too.
5. Manual labels in string with '\n' separator
6. tune colors as {} -- for example, '{b3}'
7. Sample about pthread using.
8. Add help about cmake and GIF/JPEG to the "Installation and using"
9. Regroup pictures in WWW by big classes + combo plots
10. Add '[' as separator in string
11. On which things the subplot influence
12. About unrotated text
13. Add "Howto" section in web site.
14. "Data plotting", "Data plot", "Scientific visualtization", "Surface", "Cloud","Plot", "statistical graphics" to website
15. Examples of animation, u3d, x3d, js and so on
16. Example of inverted axis, minus-log axis
17. swap color and font style in text option ('bR:r' -> 'r:bR')
18. Surf3A -- data a can be vector with size as number of isosurfaces
19. Rename 'roman' font style to 'regular'
20. Venn() diagram sample
21. '@' -- draw boxed text.
22. Rhomb() can have gradient if 3 colors is specified (1st, border, 2nd); Ellipse() and Rhomb() parse '#' and '@' flags
23. Pipes in mglview, mglconv (stdin)
24. Parallelization:
	* user level -- SetDrawReg(), PutDrawReg()
	* MPI level -- Combine()
	* MathGL level -- pthread in mglData, ???
25. Allow interpolation in SubData and in MGL if argument is not integer
26. Error() draw "big" transpared marks if '@' is specified
27. 'half-transparent' -> 'semitransparent'

============= BUGS =============

1. утилиты (mgl2png и пр.) вылетают, если в коментариях русские буквы
2. Last ' isn't send as call argument!!!
3. "Chaos" in area.png (near axis) ???

